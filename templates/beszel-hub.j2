[Unit]
Description=Beszel Hub
After=network.target

[Service]
Type=simple
Restart=always
RestartSec=3
User=root
WorkingDirectory={{ beszel_workdir }}
ExecStart={{ beszel_workdir }}/beszel serve --http "0.0.0.0:8090"

[Install]
WantedBy=multi-user.target